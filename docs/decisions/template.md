# ADR NNNN: [Short, descriptive title of the decision]

## Status
[Proposed | Accepted | Superseded — if superseded, link the ADR that replaces this one]

## Context
[One or two paragraphs: what problem forced this decision? What was the
prior state (if any), and why did it stop being adequate? Reference
ROADMAP.md or a prior ADR if this decision was scoped/triggered by one.]

## Decision
[What was actually decided and built. Be specific about the contract/
interface (function signatures, file locations, data shapes) so future
readers know exactly what changed and what the swap-in/extension points
are, if any.]

## Validation
[How was this decision checked against real data/behavior, not just
reasoning? Include concrete test cases, commands run, and actual output —
not estimated or expected numbers. If something wasn't validated yet,
say so explicitly rather than omitting the section.]

## Consequences
[What does this decision commit us to? New dependencies, known
limitations, follow-up work, and anything that could break or need
revisiting later. Be honest about trade-offs and open issues — this
section should read the same whether the decision worked out cleanly or
not.]

## Cross-reference
[Optional. Link related ADRs this one builds on, completes, or supersedes.]
